# macOS Finder metadata, written into any folder that gets opened
.DS_Store

# Scratch space: reference decks copied in to compare against, exported PDFs,
# screenshots dropped in on the way to assets/. Nothing here is part of a deck.
tmp/
